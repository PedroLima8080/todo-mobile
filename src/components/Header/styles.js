import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    ContentHeader: {
        marginTop: 10
    },
    TitleHeader: {
        color: '#616161',
        textAlign: 'center'
    }
})

export default styles