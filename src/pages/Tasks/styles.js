import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    Alert: {
        textAlign: 'center',
        marginTop: 10,
        fontSize: 18
    },
    SpanTopic: {
        color: '#616161',
        fontSize: 14,
        marginBottom: 10
    }
})

export default styles