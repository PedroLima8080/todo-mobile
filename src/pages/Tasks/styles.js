import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    Alert: {
        textAlign: 'center',
        marginTop: 10,
        fontSize: 14
    },
    SpanTopic: {
        color: '#616161',
        fontSize: 16,
        marginBottom: 10,
        letterSpacing: 1
    }
})

export default styles